"""Evaluation metrics for structure learning, classification, and effect estimation.

Includes structural metrics (SHD, F1, edge precision/recall via
`utils.metrics`) and treatment-effect metrics (PEHE, ATE bias, CATE
calibration via `effect_metrics`).
"""
